{
  "name": "shared-backend",
  "version": "1.0.0",
  "description": "the shared backend",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "rimraf dist && node ./esbuild.config.js && tsc --emitDeclarationOnly --outDir dist"
  },
  "dependencies": {
    "@med-soc/codegen-sdk": "^1.0.0"
  }
}
